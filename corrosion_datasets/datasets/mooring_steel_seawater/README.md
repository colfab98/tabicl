# Mooring Steel In Synthetic Seawater

Source and raw-file provenance are in `manifest.yml`. The inspected archive schema, feature groups, targets, leakage notes, and prior relevance are in `schema_summary.md`.

The raw PDF and data ZIP are stored under `raw/`.
